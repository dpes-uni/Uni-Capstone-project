# Copy to .env and adjust if needed.
# Left blank / "/api" -> requests go through the Vite dev proxy to
# http://localhost:5000 (see vite.config.js). Set an absolute URL here if
# you run the backend somewhere else (e.g. in production).
VITE_API_URL=/api
